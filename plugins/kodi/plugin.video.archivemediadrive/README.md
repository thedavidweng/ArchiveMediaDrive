# Kodi adapter

This is a source-only Kodi Python plugin scaffold. Release packaging vendors the official `internetarchive` package source and its pure-Python dependencies. It must pass Kodi addon-checker and contain no compiled binaries.
