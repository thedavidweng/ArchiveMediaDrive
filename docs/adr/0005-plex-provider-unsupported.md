# ADR 0005: Do not ship a Plex legacy provider

Status: Accepted

Current Plex releases have removed plugin-based content playback and are retiring the legacy plugin system. ArchiveMediaDrive will not ship a `.bundle` Channel or claim native Plex provider support.

The decision can be revisited only after Plex publishes a supported third-party content-source API.
